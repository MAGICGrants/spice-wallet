// ThemeModel lives in wallet-core (wallet_infra); kept under this path so call
// sites are unchanged. The app maps ThemeModel.theme -> ThemeMode itself.
export 'package:wallet_infra/wallet_infra.dart' show ThemeModel;
