// SecureScreenMixin lives in wallet-core (wallet_infra); kept under this path so
// call sites (seed/keys screens) are unchanged.
export 'package:wallet_infra/wallet_infra.dart' show SecureScreenMixin;
